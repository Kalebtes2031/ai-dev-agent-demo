# Development Rules

- Keep the project structure clean and minimal.
- Do not implement features without explicit user request.
- Prefer small, well-named modules over monolithic files.
- Add dependencies deliberately and document them in `requirements.txt`.
- Write clear, user-facing run instructions in `README.md`.
- Avoid committing secrets or local data.
- When unsure, ask before making architectural changes.
